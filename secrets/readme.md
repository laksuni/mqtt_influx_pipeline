# Secrets
This folder must contain the following files (not committed):
- `influx_token.txt` – your very long  InfluxDB authentication token
- `mqtt_username.txt` – MQTT username
- `mqtt_password.txt` – MQTT password
